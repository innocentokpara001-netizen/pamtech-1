# Optional Supabase values for future backend features.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Add this in Netlify environment variables only when server routes need it.
SUPABASE_SERVICE_ROLE_KEY=
